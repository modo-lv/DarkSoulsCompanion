#WeaponFinder

	table.params
		tr.limits
			th.left Limits:

			td
				label(ng-repeat="(name,value) in params.reqLimits")
					| {{name | statName}}:
					input(type="number" ng-model="$parent.params.reqLimits[name]" min=0 max=99)
				button(ng-click="copyStatsToReqs()" title="Use current player stats.")
					img(src="/app/content/menu-icons/pc.png")

		tr.misc
			th.left Misc.:
			td
				label
					input(type="checkbox" ng-model="params.includeUpgrades")
					| Include available upgrades


	button(ng-click="findWeapons()")
		| Find
		select(ng-click="$event.stopPropagation()" ng-model="params.searchType")
			option(value="offence") weapons
			option(value="defence") shields

	div(ui-grid-auto-resize ui-grid="gridOptions")