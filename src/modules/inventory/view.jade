#Inventory
	angucomplete-alt#AddToInventory(
		local-data="allItems"
		selected-object="addNewItem"
		title-field="name"
		search-fields="name"
		clear-selected=true
		placeholder="Add item..."
		minlength=1
	)

	angucomplete-alt#AddArmorSet(
		local-data="armorSets"
		selected-object="addArmorSet"
		title-field="name"
		search-fields="name"
		clear-selected="true"
		placeholder="Add armor set..."
		minlength=1
	)


	// Grid
	div(ui-grid-auto-resize ui-grid="gridOptions")

	// Grid action buttons
	script(id="GridRowHeader.html" type="text/ng-template")
		button(ng-click="grid.appScope.addItem(row.entity)") +
		button(ng-click="grid.appScope.removeItem(row.entity)") −